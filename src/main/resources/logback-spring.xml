<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Propiedad para el nombre de la aplicación -->
    <property name="friday.bbf.back" value="my-application"/>

    <!-- Propiedad para el patrón del log -->
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- File Appender con rotación -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/${APP_NAME}.log</file>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Rotación diaria y por tamaño -->
            <fileNamePattern>logs/${APP_NAME}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!-- Tamaño máximo por archivo -->
            <maxFileSize>10MB</maxFileSize>
            <!-- Máximo de días a conservar -->
            <maxHistory>30</maxHistory>
            <!-- Tamaño total máximo -->
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Logger levels -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Ejemplo de logger específico -->
    <logger name="com.bbf.friday.bbf.back" level="DEBUG"/>

    <!-- Para desactivar logs innecesarios -->
    <logger name="org.hibernate" level="WARN"/>
    <logger name="org.springframework" level="WARN"/>
</configuration>